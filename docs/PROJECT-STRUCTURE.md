# 项目目录结构

> 仓库：[https://github.com/webmturn/ai-localization](https://github.com/webmturn/ai-localization)

## 📁 目录说明

```
html/
├── config/              # 配置文件目录
│   ├── cdn-versions.json    # CDN 资源版本配置
│   ├── common-terms-50.json # 可选：初始/示例术语数据
│   └── tailwind.config.js   # Tailwind CSS 配置
│
├── docs/                # 文档目录
│   ├── INDEX.md                     # 文档索引（入口）
│   ├── API-REFERENCE.md             # 核心服务和工具 API 参考
│   ├── APP-JS-Function-Guide.md     # app 模块与函数说明（维护/二次开发）
│   ├── ARCHITECTURE-USAGE-GUIDE.md  # 架构系统使用指南
│   ├── ERROR-HANDLING-GUIDE.md      # 错误处理指南
│   ├── ERROR-HANDLING-QUICK-START.md # 错误处理快速开始
│   ├── ERROR-HANDLING-USAGE-EXAMPLES.md # 错误处理使用示例
│   ├── GITHUB-RELEASE-CHECKLIST.md  # GitHub 发布前检查清单
│   ├── NodeJS-Install-Guide.md      # Node.js 安装指南
│   ├── PROJECT-STRUCTURE.md         # 项目结构说明（本文件）
│   ├── QUICK-START.md               # 快速上手指南
│   ├── README-CDN-UPDATE.md         # CDN 更新指南
│   ├── README-TAILWIND.md           # Tailwind CSS 本地安装与使用说明
│   ├── TESTING-AND-PRODUCTION.md    # 测试与生产环境
│   └── history/                     # 归档/过程文档（修复报告、分析记录等）
│       └── README.md               # 归档说明
│
├── scripts/             # 脚本目录
│   ├── tools.ps1                # 整合脚本（Node 检查 / 版本检查 / 配置更新 / CDN 下载）
│   ├── update-cdn.ps1           # CDN 更新入口（调用 tools.ps1）
│   ├── check-latest-versions.ps1 # 版本检查入口（调用 tools.ps1）
│   ├── auto-update-versions.ps1 # 自动更新配置入口（调用 tools.ps1）
│   ├── check-node-install.ps1      # Node 安装检查入口（调用 tools.ps1，仅检查不安装）
│   └── split-app-js.ps1         # 单体 app.js 拆分为模块（独立，不整合）
│
├── src/                 # 源代码目录
│   └── input.css        # Tailwind CSS 输入文件
│
├── public/              # 发布目录（浏览器打开/部署）
│   ├── lib/                 # 第三方库目录（本地化资源）
│   │   ├── chart.js/
│   │   │   └── chart.umd.min.js
│   │   ├── font-awesome/
│   │   │   ├── font-awesome.min.css
│   │   │   └── fonts/
│   │   │       └── fontawesome-webfont.woff2
│   │   └── sheetjs/
│   │       └── xlsx.full.min.js
│   ├── index.html           # 主 HTML 文件
│   ├── app.js               # 应用入口（按顺序加载 public/app/**）
│   ├── app/                 # 应用核心逻辑（模块化代码）
│   └── styles.css           # 构建后的 CSS 文件（由 Tailwind 生成）
├── package.json         # npm 项目配置
└── .gitignore           # Git 忽略文件配置
```

## 目录详细说明

### `config/` - 配置文件
存放所有配置文件，便于管理和维护。

- **`cdn-versions.json`**: 管理第三方库的版本和 CDN URL
- **`common-terms-50.json`**: 可选，初始/示例术语数据
- **`tailwind.config.js`**: Tailwind CSS 的自定义配置

### `docs/` - 文档
存放所有项目文档和说明文件。包含快速开始、项目结构、Node.js 安装、CDN 更新、Tailwind 使用、GitHub 发布清单、app 函数说明、项目分析报告及存储方案草案等。

### `scripts/` - 脚本
存放所有 PowerShell 脚本，用于自动化任务。除 `split-app-js.ps1` 外，其余逻辑已整合到 **`tools.ps1`**，其它脚本为入口包装。

- **`tools.ps1`**: 整合脚本，支持 `-Action NodeCheck|CheckVersions|UpdateConfig|UpdateCdn|All`
- **`update-cdn.ps1`**: CDN 更新入口（可传 `-CheckOnly` 仅查版本）
- **`check-latest-versions.ps1`**: 检查第三方库最新版本
- **`auto-update-versions.ps1`**: 将 cdn-versions.json 更新为最新版本号
- **`check-node-install.ps1`**: Node.js 安装检查与指导（仅检查是否已安装，不执行安装）
- **`split-app-js.ps1`**: 将单体 app.js 拆分为模块（需在项目根目录执行）
- **`.bat` 文件**（若存在）：双击即用，如 `check-node-install.bat`、`update-cdn.bat` 等，可自行添加以包装上述 ps1 脚本

### `src/` - 源代码
存放源代码文件。

- **`input.css`**: Tailwind CSS 的输入文件，包含所有自定义样式

### `public/` - 发布目录
存放浏览器直接打开/部署所需的所有文件。

### `public/lib/` - 第三方库
存放本地化的第三方库资源，避免依赖 CDN。

## 🚀 常用命令

### 构建 CSS
```bash
npm run build-css
```

### 监听 CSS 变化（开发模式）
```bash
npm run watch-css
```

### 更新 CDN 资源
```bash
npm run update-cdn
```

### 检查最新版本
```bash
npm run check-versions
```

### 自动更新到最新版本
```bash
npm run auto-update
```

## 📝 文件说明

### 根目录文件

- **`public/index.html`**: 应用的主 HTML 文件
- **`public/app.js`**: 应用入口（按顺序加载 `public/app/**`）
- **`public/app/`**: 应用核心逻辑（模块化代码）
- **`public/styles.css`**: 由 Tailwind CSS 构建生成的样式文件（自动生成，不要手动编辑）
- **`package.json`**: npm 项目配置和脚本定义
- **`.gitignore`**: Git 版本控制忽略文件配置

## 🔄 工作流程

1. **开发时**:
   - 编辑 `src/input.css` 添加自定义样式
   - 运行 `npm run watch-css` 监听变化
   - 编辑 `public/index.html` 和 `public/app/` 下对应模块（如需调整加载顺序再改 `public/app.js`）

2. **更新第三方库**:
   - 运行 `npm run check-versions` 查看是否有更新
   - 运行 `npm run auto-update` 自动更新

3. **生产构建**:
   - 运行 `npm run build-css` 生成优化后的 CSS
   - 部署 `public/` 目录

## 📌 注意事项

1. **不要手动编辑 `public/styles.css`**: 这个文件是自动生成的，修改会在下次构建时丢失
2. **编辑 `src/input.css`**: 所有自定义样式应该添加到这里
3. **配置文件位置**: 所有配置文件都在 `config/` 目录下
4. **脚本路径**: 所有脚本都在 `scripts/` 目录下，通过 npm 脚本调用


