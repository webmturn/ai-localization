# CDN 资源更新指南

本项目使用本地化的第三方库资源，但保留了从 CDN 更新的功能。

## 📦 当前使用的库版本

- **Font Awesome**: v4.7.0
- **Chart.js**: v4.5.1
- **SheetJS**: v0.20.1

## 🚀 更新方法

### ⚡ 方法 1: 自动检测并更新（推荐）

自动检测最新版本并更新：

```bash
npm run auto-update
```

这会：
1. 自动检测所有库的最新版本
2. 自动更新 `cdn-versions.json`
3. 自动下载最新资源

### 📊 方法 2: 仅检查最新版本

检查是否有新版本可用（不更新）：

```bash
npm run check-versions
```

### 🔄 方法 3: 手动更新（使用当前配置）

使用 `cdn-versions.json` 中的版本更新：

```bash
npm run update-cdn
```

### 📝 方法 4: 仅检查版本信息

查看当前配置的版本信息：

```bash
npm run update-cdn -- -CheckOnly
```

## ⚙️ 手动配置版本（可选）

如果需要手动指定特定版本，可以编辑 `cdn-versions.json` 文件：

```json
{
  "font-awesome": {
    "version": "4.7.0",  // 修改版本号
    "css": "https://cdn.jsdelivr.net/npm/font-awesome@4.7.0/css/font-awesome.min.css",
    "font": "https://cdn.jsdelivr.net/npm/font-awesome@4.7.0/fonts/fontawesome-webfont.woff2",
    ...
  },
  "chart.js": {
    "version": "4.5.1",  // 修改版本号
    "url": "https://cdn.jsdelivr.net/npm/chart.js@4.5.1/dist/chart.umd.min.js",
    ...
  },
  "sheetjs": {
    "version": "0.20.1",  // 修改版本号
    "url": "https://cdn.sheetjs.com/xlsx-0.20.1/package/dist/xlsx.full.min.js",
    ...
  }
}
```

然后运行更新脚本：

```bash
npm run update-cdn
```

**注意**：推荐使用自动更新方法，无需手动修改版本号。

## 📝 更新流程

1. **检查新版本**
   - 访问各库的官方网站查看最新版本
   - Font Awesome: https://fontawesome.com/
   - Chart.js: https://www.chartjs.org/
   - SheetJS: https://sheetjs.com/

2. **更新配置文件**
   - 编辑 `cdn-versions.json`
   - 修改版本号和对应的 CDN URL

3. **运行更新脚本**
   ```bash
   npm run update-cdn
   ```

4. **测试更新后的资源**
   - 刷新浏览器页面
   - 检查控制台是否有错误
   - 测试相关功能是否正常

## ⚠️ 注意事项

1. **版本兼容性**
   - 更新前请查看各库的更新日志
   - 确保新版本与现有代码兼容
   - 建议先在测试环境验证

2. **Font Awesome 特殊处理**
   - 脚本会自动修复字体路径
   - 自动添加 `font-display: swap` 优化
   - 只保留 woff2 格式以减小文件大小

3. **备份建议**
   - 更新前建议备份 `public/lib/` 目录
   - 如果出现问题，可以快速恢复

## 🔍 验证更新

更新后，检查以下内容：

1. **文件大小**
   ```powershell
   Get-ChildItem public\lib -Recurse | Select-Object Name, @{Name="Size(KB)";Expression={[math]::Round($_.Length/1KB,2)}}
   ```

2. **浏览器控制台**
   - 无错误信息
   - 无跟踪防护警告
   - 字体正常加载

3. **功能测试**
   - 图标显示正常（Font Awesome）
   - 图表功能正常（Chart.js）
   - Excel 导出功能正常（SheetJS）

## 📚 相关资源

- [Font Awesome 文档](https://fontawesome.com/v4.7.0/)
- [Chart.js 文档](https://www.chartjs.org/docs/latest/)
- [SheetJS 文档](https://docs.sheetjs.com/)

## 🆘 故障排除

### 更新失败

如果更新失败，检查：
1. 网络连接是否正常
2. CDN URL 是否正确
3. 文件路径是否有写入权限

### 更新后功能异常

1. 检查浏览器控制台错误
2. 查看各库的更新日志
3. 回退到之前的版本
4. 检查代码是否需要适配新版本

### 回退版本

1. 从备份恢复 `lib/` 目录
2. 或修改 `cdn-versions.json` 到之前的版本
3. 重新运行更新脚本

